<UserControl x:Class="Jiandanmao.Uc.CateringOrder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:smtx="clr-namespace:ShowMeTheXAML;assembly=ShowMeTheXAML"
             xmlns:local="clr-namespace:Jiandanmao.Uc"
             xmlns:convert="clr-namespace:Jiandanmao.Converter"
             mc:Ignorable="d" 
             x:Name="CateringOrderControl"
             d:DesignHeight="450" d:DesignWidth="800">
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding StoreOrderLoadedCommand}" CommandParameter="{Binding ElementName=CateringOrderControl}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ListView.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <convert:DeskQuantityTypeConverter x:Key="dqtc" />
            <convert:DateTimeToStringValueConverter x:Key="dtts" />
            <convert:StoreOrderOperate1TypeConverter x:Key="sootc1" />
            <convert:StoreOrderOperate2TypeConverter x:Key="sootc2" />

            <Style TargetType="{x:Type TextBlock}">
                <Setter Property="FontSize" Value="18"/>
            </Style>

        </ResourceDictionary>

    </UserControl.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350" />
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <!--已点菜品-->
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="80" />
                <RowDefinition Height="*" />
                <RowDefinition Height="20" />
                <RowDefinition Height="80" />
            </Grid.RowDefinitions>
            <Grid Background="#ff5722">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Style="{DynamicResource MaterialDesignToolButton}">
                    <materialDesign:PackIcon Kind="Magnify" Opacity=".56" Foreground="White" />
                </Button>
                <TextBox Grid.Column="1" Margin="8 0 0 0" x:Name="txtProductkey" materialDesign:HintAssist.Hint="条码/助记码" materialDesign:TextFieldAssist.DecorationVisibility="Hidden" BorderThickness="0" Foreground="White"  MinWidth="250" VerticalAlignment="Center" >
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="TextChanged">
                            <i:InvokeCommandAction Command="{Binding ProductSearchCommand}" CommandParameter="{Binding ElementName=txtProductkey}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </TextBox>
                <Button Style="{DynamicResource MaterialDesignToolButton}" Grid.Column="2" Command="{Binding ClearProductKeyCommand}" CommandParameter="{Binding ElementName=txtProductkey}">
                    <materialDesign:PackIcon Kind="CloseCircle" Opacity=".56" Margin="8 0 0 0" Foreground="White" />
                </Button>
            </Grid>
            <Border Grid.Row="1" BorderBrush="#ddd" BorderThickness="1">
                <StackPanel>
                    <Grid Margin="8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="175"/>
                            <ColumnDefinition Width="175"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock>餐桌：</TextBlock>
                            <TextBlock Text="{Binding Desk.Name}"></TextBlock>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Grid.Column="1">
                            <TextBlock>人数：</TextBlock>
                            <TextBlock Text="{Binding Desk.Order.PeopleQuantity}"></TextBlock>
                        </StackPanel>
                    </Grid>
                    <StackPanel Margin="8" Orientation="Horizontal" Grid.Column="1">
                        <TextBlock>开台时间：</TextBlock>
                        <TextBlock Text="{Binding Desk.Order.CreateTime, Converter={StaticResource dtts}}"></TextBlock>
                    </StackPanel>
                </StackPanel>
            </Border>
            <Border Grid.Row="2" BorderBrush="#ddd" BorderThickness="1">

            </Border>
            <Border Grid.Row="3" BorderBrush="#ddd" BorderThickness="1">

            </Border>
            <Grid Grid.Row="4" Visibility="{Binding Desk.Order, Converter={StaticResource sootc1}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Height="70" Command="{Binding BackDeskCommand}">
                    <Button.Content>
                        <TextBlock>返回餐桌</TextBlock>
                    </Button.Content>
                </Button>
                <Button Grid.Column="1" Height="70" Command="{Binding DeleteOrderCommand}" CommandParameter="{Binding Desk}">
                    <Button.Content>
                        <TextBlock>整单删除</TextBlock>
                    </Button.Content>
                </Button>
                <Button Grid.Column="2" Height="70">
                    <Button.Content>
                        <TextBlock>下单</TextBlock>
                    </Button.Content>
                </Button>
            </Grid>
            <Grid Grid.Row="4" Visibility="{Binding Desk.Order, Converter={StaticResource sootc2}}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Height="70" Command="{Binding BackDeskCommand}">
                    <Button.Content>
                        <TextBlock>返回餐桌</TextBlock>
                    </Button.Content>
                </Button>
                <Button Grid.Column="1" Height="70" Command="{Binding AddProductCommand}">
                    <Button.Content>
                        <TextBlock>加菜</TextBlock>
                    </Button.Content>
                </Button>
                <Button Grid.Column="2" Height="70" Command="{Binding PayCommand}">
                    <Button.Content>
                        <TextBlock>结算</TextBlock>
                    </Button.Content>
                </Button>
            </Grid>
        </Grid>
        <!--功能区-->
        <smtx:XamlDisplay Key="transitions2" Height="Auto" Grid.Column="1">
            <materialDesign:Transitioner SelectedIndex="0" AutoApplyTransitionOrigins="True" x:Name="transition2">
                <!--选择菜品-->
                <materialDesign:TransitionerSlide OpeningEffect="{materialDesign:TransitionEffect FadeIn}">
                    <local:CateringProduct DataContext="{Binding}" />
                </materialDesign:TransitionerSlide>

                <!--结账-->
                <materialDesign:TransitionerSlide OpeningEffect="{materialDesign:TransitionEffect SlideInFromLeft}">
                    <local:CateringPayment DataContext="{Binding}" />
                </materialDesign:TransitionerSlide>

            </materialDesign:Transitioner>
        </smtx:XamlDisplay>
    </Grid>
</UserControl>
